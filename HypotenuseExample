class Main {
  public static void main(String[] args) {
    double x, y, z;

    x = 3;
    y = 4;

    z = Math.sqrt(x*x + y*y); 
    // note: sqrt() is called by preceding it with the class of which it is a member

    System.out.println("Hypotenuse is " + z);

  }
}
